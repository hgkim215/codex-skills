interface:
  display_name: "Ralph Execute"
  short_description: "Implement plans with goal checkpoints"
  default_prompt: "Use $ralph-execute to implement an approved plan, check goal alignment, run worker-first assessment for non-trivial work, prefer tmux-visible workers when safe splits exist, and verify the result."
