interface:
  display_name: "Ralplan"
  short_description: "Plan execution and macro goals"
  default_prompt: "Use $ralplan to turn clarified requirements and analysis into an execution-ready plan, create one macro goal if explicitly requested, and include a goal contract plus worker visibility decision."
